# P1 Isolated Harness Run - 2026-09-13T00:22:32Z

Status: **PASS (isolated dirty-tree candidate evidence only)**

This run exercised the current Repo A and Repo B working trees against the
loopback PostgreSQL database `127.0.0.1:55432/lasalle_test`. It created schema
`p1_harness_7d29a3aae670` and reported `schema_dropped=true`. No production
endpoint, credential, source activation, or hosted staging target was used.

## Command

```text
py -3.13 scripts/run_p1_isolated_harness.py --database-url postgresql://postgres@127.0.0.1:55432/lasalle_test --api-repo ../lasalle-notices-automation --api-python ../lasalle-notices-automation/.venv/Scripts/python.exe --output-dir docs/evidence/production-only-2026-09-12/p1-isolated-20260913T002232Z --arrival-rate-per-hour 22 --storage-budget-bytes 1073741824 --warm-samples 5
```

## Correctness

- Real worker termination/recovery, lost candidate ACK replay, and lost finish
  ACK replay passed. Exact finish replay returned HTTP 200.
- Changed content for one stable source identity incremented the durable-work
  revision, requeued processing, returned `updated`, and retained one logical
  candidate, edital, and principal document with the new content hash.
- The oldest poison item backed off while the next eligible item completed,
  then reached quarantine after three bounded failed attempts with
  `processing_failed` retained.

## Capacity

- The aggregate cap admitted three claims and rejected one with
  `capacity_full`.
- All 36 measured API calls avoided 5xx/transport failures and timeouts.
- Warm catalog p95 was 22.77 ms; peak database use was 4 of 97 usable
  connections; declared 1 GiB storage headroom was 98.75%.
- Twelve descriptors drained in 0.946 seconds, measuring 45,657.3 per hour
  against the declared arrival rate of 22 per hour. Peak measured RSS was
  36,335,616 bytes.

The complete sanitized report is
[`p1-isolated-harness.json`](p1-isolated-20260913T002232Z/p1-isolated-harness.json)
with its generated [`summary.txt`](p1-isolated-20260913T002232Z/summary.txt).

This evidence is not tied to frozen commit SHAs. It does not certify genuine
image-only PDF OCR fidelity, third-party availability, production drain/fence
execution, or any production mutation.
