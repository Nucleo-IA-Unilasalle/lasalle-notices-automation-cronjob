# P1 Isolated Harness Run - 2026-09-13T00:08:02Z

Status: **PASS (isolated dirty-tree candidate evidence only)**

This rerun exercised the current Repo A and Repo B working trees against the
loopback PostgreSQL database `127.0.0.1:55432/lasalle_test`. It created schema
`p1_harness_9345a67367eb` and reported `schema_dropped=true`. No production
endpoint, credential, source activation, or hosted staging target was used.

## Command

```text
py -3.13 scripts/run_p1_isolated_harness.py --database-url postgresql://postgres@127.0.0.1:55432/lasalle_test --api-repo ../lasalle-notices-automation --api-python ../lasalle-notices-automation/.venv/Scripts/python.exe --output-dir docs/evidence/production-only-2026-09-12/p1-isolated-20260913T0015Z --arrival-rate-per-hour 22 --storage-budget-bytes 1073741824 --warm-samples 5
```

## Results

- Real worker termination/recovery, lost candidate ACK replay, and lost finish
  ACK replay passed. Exact finish replay returned HTTP 200.
- The aggregate cap admitted three claims and rejected one with
  `capacity_full`.
- All 36 measured API calls avoided 5xx/transport failures and timeouts.
- Warm catalog p95 was 23.46 ms; peak database use was 5 of 97 usable
  connections; declared 1 GiB storage headroom was 98.76%.
- Twelve descriptors drained in 0.891 seconds, measuring 48,501.1 per hour
  against the declared arrival rate of 22 per hour. Peak measured RSS was
  35,356,672 bytes.

The complete sanitized report is
[`p1-isolated-harness.json`](p1-isolated-20260913T0015Z/p1-isolated-harness.json)
with its generated [`summary.txt`](p1-isolated-20260913T0015Z/summary.txt).

This evidence is not tied to frozen commit SHAs. It does not certify
image-only PDF OCR fidelity, changed-content replay, third-party availability,
production drain/fence execution, or any production mutation.
