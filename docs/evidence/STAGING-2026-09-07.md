# Hosted Staging - 2026-09-07

Status: partial verification, not production release or all-source signoff.
RR-01 through RR-05 remain OPEN. No production source lifecycle, secrets,
schedules, database or deployment was changed.

## Environment

- Reused existing Render API `lasalle-notices-api-staging`, frontend
  `lasalle-notices-staging`, and branch `staging/opportunity-sources`.
- Both deployed A commit `e9422dca8cd9c77f4bf23bc89d7e211ae8bcc693`.
  API deploy `dep-dafiamh7lnhs73fppjfg`; frontend deploy
  `dep-dafib14tnmjc73aqqrng`; both reached live status.
- Old hosted database returned 404. Replaced it with free PostgreSQL 17
  `lasalle-notices-staging-db`; expires **2026-10-07**. No paid upgrade approved.
- Explicit schema v3 migration completed before deployment. Seeded catalog
  fixtures are marked STAGING FIXTURE ONLY; no production users or records
  were copied to hosted staging. The earlier production restore stays local.
- Generated a separate staging worker secret. Legacy scheduler and source-run
  maintenance disabled; compatible claim enforcement; source catalog enabled.
- Frontend source-transparency flag enabled; API URL remains the staging API.
- Worker B `4ed363049034e5871db2d2f67b05f22aedb25274` ran locally against hosted A.
  These runs do not prove GitHub runner behavior or scheduled execution.

## Executed Checks

- Hosted live/pinned catalog parity passed. Sanitized observation retained at
  workspace `artifacts/rollout/database-rehearsal/source-reliability-2026-09-07-hosted-catalog.json`.
- BNDES adapter audit: FAILED, 26 blockers (6 duplicate_identity,
  20 identity_mismatch), nine candidates. No BNDES ingestion performed.
  Staging source-run ID `700bce1d-9358-47eb-96a2-cc9b827d86b3` persisted the
  failure and blocker count. Investigate identity and parent/attachment
  modeling; do not weaken the verifier to obtain a pass.
- BRDE adapter audit: passed with one candidate and no blockers. This is an
  adapter self-audit, NOT either of the two independent release audits.
  Run `731343ca-453c-4f2c-86a4-7e29c63e5dc3`.
- BRDE real managed ingestion: downloaded 278314 PDF bytes, extracted content,
  submitted and acknowledged one inserted edital; durable item accepted.
  Run `da824048-181e-4653-9570-096ef1177073`: success, downloaded=1,
  ocr_succeeded=1 (the extraction counter), submitted=1, inserted=1.
  This does not establish image-only PaddleOCR coverage.
- BRDE forced unchanged replay: success, no second download, extraction or
  submission; database still contains exactly one edital.
  Run `204538a5-9ac5-469d-a062-3618967c29e5`: all processing counters zero.
- Four concurrent HTTP source claims: exactly three admitted and one denied
  `capacity_full`; all acquired claims released with noop. This proves the
  live admission boundary, not long-duration capacity or latency SLOs.
- Adapter artifacts retained locally in ignored
  `artifacts/staging-20260907/{bndes-audit,brde-audit,brde-ingest,brde-replay}`.
  Do not treat missing ingestion artifact files as completed evidence.

## Still Open

Independent source ground truth and two clean audits per source; BNDES repair;
structured/documentless and image-only OCR canaries; old-worker compatibility
and failure injection against hosted staging; restart/backlog recovery;
measured capacity/latency; browser-authenticated UI verification; monitoring
recipient/operator checks; controlled production cutover; final-wave 48-hour
soak and seven-day review. All existing paused/audit holds remain unchanged.
